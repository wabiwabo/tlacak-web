export type { Device, DeviceAttributes, DeviceStatus } from './model/types';
export {
  deviceKeys,
  fetchDevices,
  useDevicesQuery,
  useSaveDevice,
  useRemoveDevice,
  devicesApi,
} from './api/queries';
